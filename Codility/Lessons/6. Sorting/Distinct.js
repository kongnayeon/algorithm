// you can write to stdout for debugging purposes, e.g.
// console.log('this is a debug message');

function solution(A) {
  
    let answer = 0;
    let set = new Set(A);

    answer = set.size;
    return answer
}
